-- 1. Crear el usuario del sistema con su contraseña
CREATE USER user_clinica WITH PASSWORD 'Cl1n1c4_P51q_2026';

-- 2. Crear la base de datos asignando al nuevo usuario como propietario
CREATE DATABASE clinica_psiquiatrica OWNER user_clinica;

-- 3. Conéctate a la base de datos 'clinica_psiquiatrica' antes de continuar
\c clinica_psiquiatrica;

-- 4. Crear la tabla de usuarios
CREATE TABLE usuarios (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    email VARCHAR(255) UNIQUE NOT NULL,
    nombre VARCHAR(255) NOT NULL,
    rol VARCHAR(50) NOT NULL,
    "createdAt" TIMESTAMP NOT NULL DEFAULT now()
);

-- 5. Otorgar permisos al usuario sobre el esquema y la tabla
GRANT USAGE ON SCHEMA public TO user_clinica;
GRANT SELECT, INSERT, UPDATE, DELETE ON TABLE usuarios TO user_clinica;